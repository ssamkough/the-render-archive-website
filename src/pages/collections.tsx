import React from 'react';
import App from '../components/structure/App';

/**
 * Collections page of the app.
 */
const collections = (): React.ReactElement => (
  <App>
    <span>collections page</span>
  </App>
);

export default collections;
